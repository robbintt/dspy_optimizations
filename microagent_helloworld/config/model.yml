# Configuration for the HelloWorld demo's LLM provider via LiteLLM

model:
  # The model identifier for litellm
  # Examples: "gpt-3.5-turbo", "anthropic/claude-3-haiku-20240307"
  name: "gpt-3.5-turbo"

# Model parameters to pass to litellm
parameters:
  temperature: 0.1
  max_tokens: 50
