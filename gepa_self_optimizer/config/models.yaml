
# Configuration for the primary task model.
task_model:
  name: "zai-glm-4.6"
  provider: "cerebras"
  temperature: 0.7
  max_tokens: 4096

# Configuration for the stronger reflection model.
# A lower temperature is used for more deterministic analysis.
reflection_model:
  name: "zai-glm-4.6"
  provider: "cerebras"
  temperature: 0.5
  max_tokens: 4096

# Configuration for the primary task model.
task_model:
  name: "zai-glm-4.6"
  provider: "cerebras"
  temperature: 0.7
  max_tokens: 4096

# Configuration for the stronger reflection model.
# A lower temperature is used for more deterministic analysis.
reflection_model:
  name: "zai-glm-4.6"
  provider: "cerebras"
  temperature: 0.5
  max_tokens: 4096
